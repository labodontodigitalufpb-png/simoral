{
  "name": "examosim",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma educacional de simulação e raciocínio clínico",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20 <26"
  }
}
